line-length = 100
target-version = "py310"

extend-exclude = [
    ".venv",
    ".venv311-forecast",
    "build",
    "dist",
    "node_modules",
    "apps/metis-web/.next",
    "apps/metis-web/out",
    "apps/metis-web-lite",
    "apps/metis-desktop",
]

# Lint rules left at ruff defaults (E + F) to match the baseline the codebase
# was passing before this file was introduced. Tighten incrementally in a
# dedicated PR rather than as part of this chore pass.
